<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bloodpair">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BloodFinder">
        <activity android:name="com.example.bloodpair.activities.home.HomeActivity"></activity>
        <activity
            android:name="com.example.bloodpair.activities.splashScreens.SplashScreenSlideActivity"
            android:theme="@style/Theme.BloodFinderSplash" />
        <activity
            android:name="com.example.bloodpair.activities.auth.LoginActivity"/>
        <activity
            android:name="com.example.bloodpair.activities.splashScreens.MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.BloodFinderSplash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>